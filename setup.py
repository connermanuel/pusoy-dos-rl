from setuptools import setup, find_packages

setup(name='pusoy',
      version='0.1',
      description='Pusoy Dos card game shenanigans.',
      packages=find_packages(),
      package_dir={'':'src'}
     )